
    
    const IconReturn = (num, icon) => {
       const data = []
        for (var i=0; i < num; i++) {
            data.push(icon)
        }
        return data
    }

    export default IconReturn
